console.debug("This accesses the `console` object and runs the `debug` function.");
console.debug("Console is a special object that browsers create to interact with the console tool.");
console.debug(The `debug` function lets you print information to the console for debugging purposes.");
